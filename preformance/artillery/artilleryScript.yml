config: 
  target: 'http://localhost:3002/'
  phases: 
    - duration: 60
      arrivalRate: 20
    - duration: 60
      arrivalRate: 50
  processor: ''

scenarios:
  - name: 'get id'
  flow:
    - function: 'randoNum'  
    - get:
      url: '/{{ id }}'
    - log: 'Sent a request to / {{ id }}'

